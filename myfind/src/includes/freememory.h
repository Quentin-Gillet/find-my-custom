#ifndef MYFIND_FREEMEMORY_H
#define MYFIND_FREEMEMORY_H

#include <stdlib.h>
#include <structures.h>

void free_all(struct node *ast, struct tokens *tokens, struct args_input *args);

#endif // MYFIND_FREEMEMORY_H
